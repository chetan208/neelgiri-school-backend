import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const teachers = await prisma.teacher.findMany(); console.log('Successfully connected and found', teachers.length, 'teachers!'); } main().catch(console.error).finally(() => prisma.$disconnect());
